version: 2.1

commands:
  print_pipeline_id:
    description: "A very simple command to suck my dick"
    parameters:
      to:
        type: string
    steps:
      - run: echo << parameters.to >>

jobs:
  say-hello:
   
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "echo Hello"
      - print_pipeline_id:
          to: "fuck me"
      - run: "return 1"
      - run:
          name: "fail alert"
          command: "echo You have fuckin failed"
          when: on_fail


workflows:
  say-hello-workflow:
    jobs:
      - say-hello
